// First import the type from the component file
import { CheckoutStepsProps } from "./CheckoutSteps";

// Then export both the component and its props type
export { default as CheckoutSteps } from "./CheckoutSteps";
export type { CheckoutStepsProps };
